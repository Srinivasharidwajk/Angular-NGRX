import { createAction, props } from '@ngrx/store';

export const checkEating = createAction(
  '[Hobby] Check Eating'
);

export const checkCoding = createAction(
  '[Hobby] Check Coding'
);

export const checkSleeping = createAction(
  '[Hobby] Check Sleeping'
);
